DATABASE_CONFIG = {
    'host': HOST_IP,
    'db': DB_NAME,
    'user': USER_NAME,
    'passwd': PASSWORD,
    'port': PORT,
    'new' : NEW_FILE_PATH,
    'old' : OLD_FILE_PATH,
    'diff' : DIFF_FILE_PATH'
}
